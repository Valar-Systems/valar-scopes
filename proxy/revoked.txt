# Revoked device ids, one per line.
#
# DO NOT WRITE A DEVICE ID IN A COMMENT. parseRevoked() splits the whole blob on
# whitespace and commas and drops only tokens that START WITH '#' -- not the rest
# of the line. Any id-shaped token anywhere in this file is a revocation,
# including one inside prose. That happened on 2026-08-31: an annotation naming
# the synthetic bench identity revoked it, and it presented as production
# refusing a correctly-derived key.
#
# Refer to devices by port or nickname here, never by id, until the parser is
# fixed. History belongs in docs/bench-key-rotation.md.
#
# 2026-08-31: list emptied. COM119 un-revoked -- its exposed key was retired by
# the DEVICE_KEY_SECRET rotation of the same day, which is remediation rather
# than the containment a revocation provides.
